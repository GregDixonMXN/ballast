import Link from "next/link";
import { Icon } from "../../components/icons";
export const metadata = { title: "Getting started" };
export default function GuidePage() { return <main id="main" className="page guide-page"><div className="page-heading"><div><span className="eyebrow">A LITTLE STRUCTURE. A LOT OF MOMENTUM.</span><h1>Your first steady step.</h1><p>Go from a local repository to a reviewed change in four steps.</p></div><Link href="/" className="button primary">Open workspace<Icon name="arrow"/></Link></div><div className="guide-layout"><section className="guide-steps"><article className="panel guide-step"><span className="step-number">01</span><div><h2>Start your local control plane</h2><p>Follow the release’s installation guide to start the Ballast server and dashboard. The default API runs on <code>127.0.0.1:8080</code>, and the dashboard on <code>127.0.0.1:3000</code>.</p><p>At startup the server creates a private operator token file. On the connection screen, select that file or paste its contents. The token stays in this tab’s memory; refreshing asks you to reconnect.</p><p className="field-help">Default source-run token path: <code>.ballast/operator.token</code>. A launcher may use a different data directory. Check your startup instructions.</p></div></article><article className="panel guide-step"><span className="step-number">02</span><div><h2>Connect a repository</h2><p>Choose <strong>New project</strong>, give it a name, and enter the absolute path to an existing local Git repository. Set the canonical branch you want to integrate into.</p><p>The repository needs at least one commit and a clean canonical worktree. Ballast leaves your source in place and creates isolated worktrees for task execution.</p></div></article><article className="panel guide-step" id="runners"><span className="step-number">03</span><div><h2>Put a runner to work</h2><p>Start the co-located runner with the same server, token file, and worktree root as the control plane. A connected runner appears in your fleet automatically.</p><p>Create a focused task with clear acceptance criteria, then choose <strong>Assign runner</strong>. Use the runner’s default adapter or select an installed adapter. You can add a verification command, such as <code>go test ./...</code>.</p><p>Runners execute with their local account’s permissions. Git worktrees isolate changes, not operating-system access. Only run trusted tasks, agents, and test commands.</p></div></article><article className="panel guide-step"><span className="step-number">04</span><div><h2>Review first. Integrate deliberately.</h2><p>A successful task produces a changeset in <strong>Reviews</strong>. Inspect every changed file and the unified diff. Check test evidence, then approve or reject the snapshot.</p><p>Approval and integration are separate actions. <strong>Integrate</strong> revalidates the snapshot against the current canonical branch. Stale or conflicting work is stopped for revision.</p><p>Workspaces are preserved after rejection or failure so you can inspect and revise the work.</p></div></article></section><aside><section className="guide-card"><Icon name="shield"/><h3>Local by design.</h3><p>The dashboard talks only to the local Ballast API. No account creation, hosted sync, analytics, or external assets are required.</p><p>Your configured coding adapter may contact its own provider. Its credentials and data handling are separate from Ballast.</p></section><section className="panel troubleshooting"><h2>If something needs attention</h2><h3>Cannot connect</h3><p>Check that the API is running and that you selected the operator token from the same instance.</p><h3>No runners online</h3><p>Start or restart the runner. After a control-plane restart, runners may need to re-register.</p><h3>Integration is blocked</h3><p>Inspect the reason, update the workspace against the new canonical head, and submit a new reviewed snapshot.</p><h3>Permissions denied</h3><p>Use an operator credential. Runner credentials cannot review or integrate changes.</p></section></aside></div></main>; }
